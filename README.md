# HeartHarborx - 心理健康AI助手

## 项目简介
HeartHarborx 是一个基于 uni-app 开发的微信小程序，专注于心理健康领域的AI助手应用。

## 功能特性

### 核心功能
- 🤖 AI心理伙伴对话
- 💬 多角色AI助手（心灵伙伴、专业顾问）
- 🎭 多种回复风格（亲切友好、专业严谨、鼓励支持、轻松随意）
- 📚 对话历史管理
- 💾 Supabase数据库集成
- 🔄 Dify API集成

### 技术特性
- 🚀 uni-app + Vue 2.7.14
- 📱 微信小程序原生支持
- 🗄️ Supabase MCP架构
- 🔐 安全的用户数据管理
- 📊 实时对话统计

## 快速开始

### 环境要求
- Node.js >= 14.0.0
- npm >= 6.0.0
- HBuilderX（推荐）或微信开发者工具

### 安装依赖
```bash
npm install
```

### 开发运行
```bash
# 开发模式
npm run dev

# 生产构建
npm run build
```

### 微信小程序配置
1. 在微信公众平台注册小程序
2. 获取 AppID: `wxdbe0ea5c455ff2b5`
3. 在微信开发者工具中导入项目
4. 配置项目路径为当前目录

## 项目结构

```
HeartHarborx/
├── pages/                 # 页面文件
│   ├── ai/               # AI聊天页面
│   ├── hole/             # 树洞页面
│   ├── index/            # 首页
│   ├── library/          # 心理库
│   └── mine/            # 个人中心
├── utils/                # 工具函数
│   └── supabase.js      # Supabase服务
├── static/              # 静态资源
├── unpackage/           # 构建输出
└── 配置文件
    ├── package.json     # 项目配置
    ├── manifest.json   # 应用配置
    ├── pages.json       # 页面路由
    └── project.config.json # 微信配置
```

## API配置

### Supabase配置
项目已配置Supabase数据库服务，用于存储用户对话数据。

**配置信息：**
- URL: `https://etvdmnsernfiegfeadad.supabase.co`
- API Key: 已配置在 `utils/supabase.js`

### Dify API配置
AI对话功能使用Dify API，配置信息在 `pages/ai/ai.vue` 中。

## 开发指南

### 添加新页面
1. 在 `pages` 目录下创建新页面文件夹
2. 在 `pages.json` 中配置页面路由
3. 在 `manifest.json` 中配置页面权限

### 数据库操作
使用 `utils/supabase.js` 中的服务类进行数据库操作：

```javascript
import conversationService from '@/utils/supabase.js'

// 创建对话
const conversation = await conversationService.createConversation(title, roleId, styleId)

// 获取用户对话
const conversations = await conversationService.getUserConversations()

// 保存消息
await conversationService.saveMessage(conversationId, role, content)
```

### 样式规范
- 使用rpx作为单位，确保多端适配
- 主色调：`#1890FF`
- 背景色：渐变蓝白 `#E6F3FF` 到 `#F5F9FF`

## 部署说明

### 微信小程序发布
1. 在微信开发者工具中点击"上传"
2. 在微信公众平台提交审核
3. 审核通过后发布

### 版本管理
项目使用Git进行版本控制，主要分支：
- `main`: 主分支，稳定版本
- `develop`: 开发分支

## 故障排除

### 常见问题
1. **依赖安装失败**
   - 清除node_modules后重新安装
   - 检查Node.js版本兼容性

2. **微信开发者工具报错**
   - 检查项目路径是否正确
   - 确认AppID配置正确

3. **API调用失败**
   - 检查网络连接
   - 验证API密钥配置

### 调试技巧
- 使用 `console.log` 进行调试
- 微信开发者工具提供详细的错误信息
- 查看网络请求面板检查API调用

## 贡献指南

欢迎提交Issue和Pull Request来改进项目！

## 许可证

本项目采用MIT许可证。

## 联系方式

如有问题或建议，请通过以下方式联系：
- 项目GitHub仓库
- 开发者邮箱

---

**注意：** 本项目为心理健康辅助工具，不能替代专业心理咨询。如有严重心理问题，请及时寻求专业帮助。